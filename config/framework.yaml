framework:
    secret: S0ME_SECRET
    profiler: { only_exceptions: false }

when@test:
    framework:
        test: true
        session:
            storage_factory_id: session.storage.factory.mock_file